# pxt-d2

Video search demo notebooks using [Pixeltable](https://github.com/pixeltable/pixeltable).

## Setup

Requires [uv](https://docs.astral.sh/uv/getting-started/installation/).

```bash
uv sync
```

Then open the notebooks in your editor or run them with:

```bash
uv run jupyter notebook
```